pages:
  image: ubuntu:latest
  only:
    - main
  script:
    - apt install -y emacs-nox
    - ./bin/build
    - mv dist public
  cache:
    key: ${CI_JOB_NAME}
    paths:
      - ./packages
  artifacts:
    paths:
      - public
